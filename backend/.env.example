# Backend environment variables
# Copy to .env for local development

# MongoDB Atlas connection string
# Get from: https://cloud.mongodb.com
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/physiocare?retryWrites=true&w=majority

# JWT secret - use a strong random string in production
JWT_SECRET=your-super-secret-jwt-key-change-this

# Port - Render sets this automatically
PORT=5000
Actually, wait - the ephemeral message says I should set a task boundary if I'm going to continue doing work. But this is just answering a question about form fields. I think I should just answer directly without overthinking it.

